let radius = 20;
let height = 88;
let cylinderVolume = Math.PI * Math.pow(radius, 2) * height;

console.log(`Volume of a cylinder with the radius of ${radius} and the height of ${height} is ${cylinderVolume.toFixed(2)}`);